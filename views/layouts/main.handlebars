<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Personality Based Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@1,700&family=Varela+Round&display=swap" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">
</head>

<body>
  <header>
    <h1 class="text-center bg-info head">Personality Based Blog</h1>

<nav class="navbar navbar-dark bg-dark justify-content-center">
   <ul class="navbar-nav d-flex flex-row">
      <li class="nav-item me-3">
        <a class="nav-link fs-5" href="/">Home</a>
      </li>
     {{#if auth}}
                 <li class="nav-item me-3">
        <a class="nav-link fs-5" href = "/{{auth}}" >{{auth}}</a>
      {{else}}
      {{/if}}
      {{#if logged_in}}
      {{else}}
      <li class="nav-item me-3">
        <a class="nav-link fs-5" href="/signup">Signup</a>
      </li>
      {{/if}}
      <li class="nav-item me-3">
         {{#if logged_in}}
         <form class="btn btn-link text-white" action="/logout" method="post" >
    <button id="logout">Logout</button>
    </form>
    {{else}}
    <a class="nav-link fs-5" href="/login">Login</a>
    {{/if}}
      </li>
</nav>
 <main>
    <div>
      {{{body}}}
    </div>
  </main>
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
  <script src="./js/javascript.js"></script>
</html>